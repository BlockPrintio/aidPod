import MeshWalletButton from './MeshWalletButton';

/**
 * Wallet Connector Component
 * Wrapper for Mesh wallet connection UI
 * Uses MeshWalletButton which is properly styled
 */
const WalletConnector = () => {
  return <MeshWalletButton />;
};

export default WalletConnector;